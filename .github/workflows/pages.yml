# Deploys the browser playground (with a freshly built ting.wasm) to
# GitHub Pages on pushes that can affect it.
name: Pages

on:
  push:
    branches: [main]
    paths:
      - "playground/**"
      - "src/**"
      - "docs/**"
      - "examples/**"
      - "lib/**"
      - "tools/md2html.ting"
      - "CHANGELOG.md"
      - "Cargo.toml"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - run: rustup target add wasm32-unknown-unknown
      - run: cargo build --release --lib --target wasm32-unknown-unknown
      - run: cp target/wasm32-unknown-unknown/release/ting.wasm playground/
      # The site's own renderer is a ting program, so the pages are
      # built by the binary this repo builds.
      - run: cargo build --release
      - name: Build the site
        run: |
          mkdir -p _site
          cp -r playground/. _site/
          ./target/release/ting tools/md2html.ting docs/tutorial.md _site/tutorial.html
          ./target/release/ting tools/md2html.ting docs/reference.md _site/reference.html
          ./target/release/ting tools/md2html.ting docs/stdlib.md _site/stdlib.html
          ./target/release/ting tools/md2html.ting docs/cookbook.md _site/cookbook.html
          ./target/release/ting tools/md2html.ting docs/retrospective.md _site/retrospective.html
          ./target/release/ting tools/md2html.ting CHANGELOG.md _site/changelog.html
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - id: deployment
        uses: actions/deploy-pages@v4
